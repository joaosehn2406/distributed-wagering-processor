# Status de implementação

Data da revisão independente e final de submissão: 05/09/2026

Esta matriz segue a hierarquia `INSTRUCOES.txt > código/testes executáveis > guia técnico > análise comparativa > arquitetura > ADR`. “Validado” exige a execução da evidência indicada; revisão estática, build e lint não são usados para marcar integração como validada.

| Requisito                                                                            | Status                         | Evidência                                                                                                                                                      | Limitação real                                                                                                                                |
| ------------------------------------------------------------------------------------ | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `Money` exato, imutável, string com duas casas e sem ponto flutuante financeiro      | IMPLEMENTADO E VALIDADO        | `src/shared/domain/money.ts`; `test/unit/money.test.ts`; `bun run build`; `bun run test:unit`                                                                  | `number` remanescente é somente porta, tempo, contador/métrica ou versão de evento; não transporta nem calcula dinheiro.                      |
| Wallet sem saldo negativo, versão correta e conflito de moeda                        | IMPLEMENTADO E VALIDADO        | `src/wallet/domain/wallet.ts`; `test/unit/wallet.test.ts`                                                                                                      | A proteção entre processos depende também de lock e constraints, pendentes de execução real.                                                  |
| Lançamento de ledger imutável e aritmeticamente válido                               | IMPLEMENTADO E VALIDADO        | `src/wallet/domain/wallet-ledger-entry.ts`; `test/unit/wallet.test.ts`; migration `0001`                                                                       | Trigger/FKs/checks do PostgreSQL ainda não foram executados nesta sessão.                                                                     |
| Wallet `0.00` sem `OPENING`, ledger ou outbox; abertura positiva atômica             | IMPLEMENTADO MAS NÃO EXECUTADO | `src/wallet/application/create-wallet.use-case.ts`; `test/integration/financial-core.test.ts`                                                                  | PostgreSQL do Compose não está disponível.                                                                                                    |
| Wager persistida antes do ledger filho; atomicidade wallet+wager+ledger+inbox+outbox | IMPLEMENTADO MAS NÃO EXECUTADO | `src/wagering/application/submit-wager-transaction.use-case.ts`; `test/integration/financial-core.test.ts`                                                     | O teste de falha por trigger não conectou ao PostgreSQL.                                                                                      |
| Locks por wallet, sem lock global e sem lost update                                  | IMPLEMENTADO MAS NÃO EXECUTADO | `WagerRepository.lockWallet`; `test/integration/concurrency.test.ts`                                                                                           | A corrida de 50 duplicatas, hot wallet e wallets independentes precisa de PostgreSQL real.                                                    |
| Constraints, índices, moeda/coerência, ledger append-only e migrations reversíveis   | IMPLEMENTADO MAS NÃO EXECUTADO | `Migration0001Initial.ts`, `Migration0002OutboxLeases.ts`, `Migration0003FinancialGuardrails.ts`, `migration-runner.ts`, `test/integration/migrations.test.ts` | `up/down/up` e constraints não executaram por indisponibilidade do banco de teste.                                                            |
| Idempotência persistente, replay idêntico e conflito de payload                      | IMPLEMENTADO MAS NÃO EXECUTADO | `canonical-hash.ts`, `submit-wager-transaction.use-case.ts`, constraints únicas, `test/integration/concurrency.test.ts`                                        | Replays sob concorrência real ainda não executados.                                                                                           |
| BET, WIN, LOSS, REFUND, ROLLBACK, reversão única e rejeição por saldo                | IMPLEMENTADO MAS NÃO EXECUTADO | `wager-transaction.ts`, `submit-wager-transaction.use-case.ts`, `test/integration/financial-core.test.ts`                                                      | A matriz financeira real, incluindo `REFERENCE_ALREADY_REVERSED`, não executou.                                                               |
| Referência pendente com TTL, tentativas, backoff e retomada                          | IMPLEMENTADO MAS NÃO EXECUTADO | `environment.ts`, `backoff.ts`, `pending-reference.worker.ts`, `distributed-delivery.test.ts`                                                                  | Requer PostgreSQL e LocalStack.                                                                                                               |
| Eventos tipados/versionados com correlação, causação e `MoneyProps`                  | IMPLEMENTADO E VALIDADO        | `integration-event.ts`, `wager-events.ts`; `test/unit/integration-events.test.ts`                                                                              | A persistência/publicação do envelope aguarda integração.                                                                                     |
| Inbox persistente, ACK após commit, retry, DLQ e shutdown de SQS                     | IMPLEMENTADO MAS NÃO EXECUTADO | `sqs-consumer.ts`, `sqs.service.ts`, `inbox-message.ts`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                      | DLQ preserva a mensagem original e registra `failureReason`/`logicalMessageId` como atributos SQS; LocalStack recusou conexão.                |
| Outbox concorrente, lease, backoff e recuperação após crash                          | IMPLEMENTADO MAS NÃO EXECUTADO | `outbox-publisher.worker.ts`, repository, migration `0002`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                   | Dois publishers reais não puderam iniciar sem LocalStack/PostgreSQL.                                                                          |
| Reconciliação exata, sem correção silenciosa                                         | IMPLEMENTADO MAS NÃO EXECUTADO | `reconcile-wallet.use-case.ts`; todos os testes de integração financeiros                                                                                      | Consulta e métrica de divergência não foram exercitadas contra banco real.                                                                    |
| DTOs, erros HTTP estáveis e cursor opaco do ledger                                   | IMPLEMENTADO E VALIDADO        | `http.dto.ts`, `http.controller.ts`, `http-error.ts`; `test/unit/http-contract.test.ts`                                                                        | Endpoints completos ainda não foram exercitados com servidor e dependências reais.                                                            |
| Logs JSON, métricas de negócio e health live/ready                                   | IMPLEMENTADO E VALIDADO        | `structured-logger.ts`, `metrics.service.ts`, `health.service.ts`; `test/unit/observability.test.ts`                                                           | Readiness real requer banco e filas em execução.                                                                                              |
| Docker Compose com API, consumer, dois publishers e pending worker                   | IMPLEMENTADO MAS NÃO EXECUTADO | `docker-compose.yml`, `Dockerfile`, `scripts/localstack/init-queues.sh`                                                                                        | Docker daemon não está ativo/acessível nesta sessão.                                                                                          |
| Três instâncias/processos, crash pós-commit/pré-ACK e restart                        | IMPLEMENTADO MAS NÃO EXECUTADO | `three-process-wallet-race.test.ts`, `process-crash-recovery.test.ts`, `concurrency.test.ts`, Compose                                                          | Três processos `APP_ROLE=all` exercitam HTTP, SQS e publisher contra os mesmos recursos; o teste depende da infraestrutura real indisponível. |
| Documentação operacional e decisões publicadas                                       | IMPLEMENTADO E VALIDADO        | `README.md`, `ARCHITECTURE.md`, `VALIDACAO_FINAL.md`, este arquivo                                                                                             | Autenticação segue explicitamente fora de escopo, com `NoopAuthGuard` como ponto de extensão; a arquitetura não depende de caminho externo.   |

## Verificações executadas nesta revisão

- `bun run build` — aprovado.
- `bun run lint` — aprovado.
- `bun run format:check` — aprovado.
- `bun run test:unit` — aprovado: 19 testes, 59 expectations, 0 falhas.
- `docker compose config --quiet` — aprovado; a configuração é válida sem iniciar containers.
- `bun run test:integration` — 0/7 cenários aprovados por infraestrutura externa: LocalStack recusou `localhost:4566` e PostgreSQL em `localhost:5432` recusou as credenciais `wager/wager`. Não houve falha de asserção de domínio observada, pois os testes não chegaram ao setup de seus recursos temporários.
- `docker version` — cliente instalado, mas o Docker Engine não está acessível em `//./pipe/dockerDesktopLinuxEngine`; por isso `docker compose up -d --build` não foi executável nesta sessão.
- Varredura estática: não há `parseFloat`, `toBeCloseTo`, `float`, `double` ou `TODO`/`FIXME` de requisito obrigatório em `src` e `test`; a composição de SQL variável usa somente fragmentos constantes e todos os valores de entrada vão como parâmetros.

## Cobertura distribuída exigida

| Cenário                                                        | Teste real planejado                                                                                                                                   | Estado desta sessão            |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ |
| 1. BET idêntica 50 vezes, um débito/ledger                     | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO MAS NÃO EXECUTADO |
| 2. Duas BET de `80.00` sobre `100.00`                          | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO MAS NÃO EXECUTADO |
| 3. Wallets diferentes em paralelo                              | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO MAS NÃO EXECUTADO |
| 4. Três processos Bun/Nest com PostgreSQL e SQS compartilhados | `three-process-wallet-race.test.ts`                                                                                                                    | IMPLEMENTADO MAS NÃO EXECUTADO |
| 5. Dois publishers concorrentes                                | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO MAS NÃO EXECUTADO |
| 6. Redelivery SQS sem segundo efeito                           | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO MAS NÃO EXECUTADO |
| 7. Envelope inválido em DLQ com razão rastreável               | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO MAS NÃO EXECUTADO |
| 8. Falha transitória sem ACK prematuro                         | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO MAS NÃO EXECUTADO |
| 9. Crash pós-commit/pré-ACK e restart                          | `process-crash-recovery.test.ts`                                                                                                                       | IMPLEMENTADO MAS NÃO EXECUTADO |
| 10. Pending reference, backoff e resolução                     | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO MAS NÃO EXECUTADO |
| 11. Restart retoma pending e outbox                            | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO MAS NÃO EXECUTADO |
| 12. Reconciliação ao fim de cada cenário financeiro            | `financial-core.test.ts`, `concurrency.test.ts`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`, `three-process-wallet-race.test.ts` | IMPLEMENTADO MAS NÃO EXECUTADO |

## Pendências objetivas

1. Iniciar PostgreSQL e LocalStack do `docker-compose.yml` em portas livres (ou definir `INTEGRATION_DATABASE_URL` e `SQS_ENDPOINT` para serviços compatíveis).
2. Executar `bun run test:integration` e corrigir qualquer resultado dinâmico.
3. Executar `docker compose up -d --build`, depois verificar `/health/live`, `/health/ready` e `/metrics` contra os serviços reais.
4. Só depois dessas evidências, promover os itens distribuídos desta matriz para `IMPLEMENTADO E VALIDADO`.
