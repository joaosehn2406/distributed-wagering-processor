# Status de implementação

Data da revisão independente e final de submissão: 05/09/2026

Esta matriz segue a hierarquia `INSTRUCOES.txt > código/testes executáveis > guia técnico > análise comparativa > arquitetura > ADR`. “Validado” exige a execução da evidência indicada; revisão estática, build e lint não são usados para marcar integração como validada.

| Requisito                                                                            | Status                  | Evidência                                                                                                                                     | Limitação real                                                                                                                              |
| ------------------------------------------------------------------------------------ | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `Money` exato, imutável, string com duas casas e sem ponto flutuante financeiro      | IMPLEMENTADO E VALIDADO | `src/shared/domain/money.ts`; `test/unit/money.test.ts`; `bun run build`; `bun run test:unit`                                                 | `number` remanescente é somente porta, tempo, contador/métrica ou versão de evento; não transporta nem calcula dinheiro.                    |
| Wallet sem saldo negativo, versão correta e conflito de moeda                        | IMPLEMENTADO E VALIDADO | `src/wallet/domain/wallet.ts`; `test/unit/wallet.test.ts`; `financial-core.test.ts`; `concurrency.test.ts`                                    | Validado contra PostgreSQL real no Compose.                                                                                                 |
| Lançamento de ledger imutável e aritmeticamente válido                               | IMPLEMENTADO E VALIDADO | `src/wallet/domain/wallet-ledger-entry.ts`; `test/unit/wallet.test.ts`; migrations; `schema-guardrails.test.ts`                               | Trigger, FKs e checks foram exercitados em PostgreSQL real.                                                                                 |
| Wallet `0.00` sem `OPENING`, ledger ou outbox; abertura positiva atômica             | IMPLEMENTADO E VALIDADO | `src/wallet/application/create-wallet.use-case.ts`; `test/integration/financial-core.test.ts`                                                 | Cenário passou contra PostgreSQL real no Compose.                                                                                           |
| Wager persistida antes do ledger filho; atomicidade wallet+wager+ledger+inbox+outbox | IMPLEMENTADO E VALIDADO | `src/wagering/application/submit-wager-transaction.use-case.ts`; `test/integration/financial-core.test.ts`                                    | Trigger de falha confirmou rollback dos efeitos atômicos.                                                                                   |
| Locks por wallet, sem lock global e sem lost update                                  | IMPLEMENTADO E VALIDADO | `WagerRepository.lockWallet`; `test/integration/concurrency.test.ts`; `three-process-wallet-race.test.ts`                                     | Corridas em três conexões e três processos passaram.                                                                                        |
| Constraints, índices, moeda/coerência, ledger append-only e migrations reversíveis   | IMPLEMENTADO E VALIDADO | `Migration0001Initial.ts`, `Migration0002OutboxLeases.ts`, `Migration0003FinancialGuardrails.ts`, `migration-runner.ts`, `migrations.test.ts` | `up/down/up`, triggers e constraints passaram em PostgreSQL real.                                                                           |
| Idempotência persistente, replay idêntico e conflito de payload                      | IMPLEMENTADO E VALIDADO | `canonical-hash.ts`, `submit-wager-transaction.use-case.ts`, constraints únicas, `concurrency.test.ts`                                        | As 50 duplicatas e replays concorrentes passaram.                                                                                           |
| BET, WIN, LOSS, REFUND, ROLLBACK, reversão única e rejeição por saldo                | IMPLEMENTADO E VALIDADO | `wager-transaction.ts`, `submit-wager-transaction.use-case.ts`, `financial-core.test.ts`                                                      | A matriz financeira, inclusive `REFERENCE_ALREADY_REVERSED`, passou.                                                                        |
| Referência pendente com TTL, tentativas, backoff e retomada                          | IMPLEMENTADO E VALIDADO | `environment.ts`, `backoff.ts`, `pending-reference.worker.ts`, `distributed-delivery.test.ts`                                                 | Pending, expiração e worker novo foram exercitados com PostgreSQL/LocalStack reais.                                                         |
| Eventos tipados/versionados com correlação, causação e `MoneyProps`                  | IMPLEMENTADO E VALIDADO | `integration-event.ts`, `wager-events.ts`; `integration-events.test.ts`; `distributed-delivery.test.ts`                                       | Envelope persistido/publicado foi exercitado pela outbox real.                                                                              |
| Inbox persistente, ACK após commit, retry, DLQ e shutdown de SQS                     | IMPLEMENTADO E VALIDADO | `sqs-consumer.ts`, `sqs.service.ts`, `inbox-message.ts`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                     | DLQ, retry, redelivery e crash real passaram no LocalStack.                                                                                 |
| Outbox concorrente, lease, backoff e recuperação após crash                          | IMPLEMENTADO E VALIDADO | `outbox-publisher.worker.ts`, repository, migration `0002`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                  | Dois publishers reais e recuperação pós-crash passaram.                                                                                     |
| Reconciliação exata, sem correção silenciosa                                         | IMPLEMENTADO E VALIDADO | `reconcile-wallet.use-case.ts`; todos os testes de integração financeiros                                                                     | Toda suíte financeira terminou com igualdade exata ledger/wallet.                                                                           |
| DTOs, erros HTTP estáveis e cursor opaco do ledger                                   | IMPLEMENTADO E VALIDADO | `http.dto.ts`, `http.controller.ts`, `http-error.ts`; `http-contract.test.ts`; `process-crash-recovery.test.ts`                               | Contrato unitário e endpoints HTTP reais passaram.                                                                                          |
| Logs JSON, métricas de negócio e health live/ready                                   | IMPLEMENTADO E VALIDADO | `structured-logger.ts`, `metrics.service.ts`, `health.service.ts`; `observability.test.ts`; Compose                                           | `/health/live`, `/health/ready` e `/metrics` responderam contra serviços reais.                                                             |
| Docker Compose com API, consumer, dois publishers e pending worker                   | IMPLEMENTADO E VALIDADO | `docker-compose.yml`, `Dockerfile`, `.env.example`, `.gitattributes`, `scripts/localstack/init-queues.sh`                                     | Stack iniciou com PostgreSQL em `55432`, LocalStack em `45666` e hook/healthcheck que espera as três filas.                                 |
| Três instâncias/processos, crash pós-commit/pré-ACK e restart                        | IMPLEMENTADO E VALIDADO | `three-process-wallet-race.test.ts`, `process-crash-recovery.test.ts`, `concurrency.test.ts`, Compose                                         | Três processos `APP_ROLE=all`, crash e restart passaram contra os mesmos recursos.                                                          |
| Documentação operacional e decisões publicadas                                       | IMPLEMENTADO E VALIDADO | `README.md`, `ARCHITECTURE.md`, `VALIDACAO_FINAL.md`, este arquivo                                                                            | Autenticação segue explicitamente fora de escopo, com `NoopAuthGuard` como ponto de extensão; a arquitetura não depende de caminho externo. |

## Verificações executadas nesta revisão

- `bun run build` — aprovado.
- `bun run lint` — aprovado.
- `bun run format:check` — aprovado.
- `bun run test:unit` — aprovado: 20 testes, 60 expectations, 0 falhas.
- `docker compose config --quiet` — aprovado.
- `docker compose up -d --build --force-recreate` — aprovado: PostgreSQL em `55432`, LocalStack em `45666`, API, consumer, dois publishers e pending worker em execução. O processo preservou volumes e não tocou no PostgreSQL externo da porta `5432`.
- `GET /health/live`, `GET /health/ready` e `GET /metrics` — aprovados contra a stack real.
- `bun run test:integration` — aprovado: 7 testes, 91 expectations, 0 falhas, executado no host contra PostgreSQL e LocalStack reais expostos pelo Compose em `55432`/`45666`.
- `bun run test:critical` — aprovado: 27 testes, 151 expectations, 0 falhas. O cenário SQS de long poll declara timeout realista de 30 s, em vez do default de 5 s do Bun.
- O healthcheck do LocalStack consulta as três filas exigidas, portanto `migrate` e workers não iniciam na janela entre o health genérico e o hook `ready.d`.
- A porta `5432` do host pertence a outro PostgreSQL local; o mapeamento não convencional e os defaults do código/teste evitam conectá-lo.
- Varredura estática: não há `parseFloat`, `toBeCloseTo`, `float`, `double` ou `TODO`/`FIXME` de requisito obrigatório em `src` e `test`; a composição de SQL variável usa somente fragmentos constantes e todos os valores de entrada vão como parâmetros.

## Cobertura distribuída exigida

| Cenário                                                        | Teste real planejado                                                                                                                                   | Estado desta sessão     |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- |
| 1. BET idêntica 50 vezes, um débito/ledger                     | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO E VALIDADO |
| 2. Duas BET de `80.00` sobre `100.00`                          | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO E VALIDADO |
| 3. Wallets diferentes em paralelo                              | `concurrency.test.ts`, `three-process-wallet-race.test.ts`                                                                                             | IMPLEMENTADO E VALIDADO |
| 4. Três processos Bun/Nest com PostgreSQL e SQS compartilhados | `three-process-wallet-race.test.ts`                                                                                                                    | IMPLEMENTADO E VALIDADO |
| 5. Dois publishers concorrentes                                | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO E VALIDADO |
| 6. Redelivery SQS sem segundo efeito                           | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO E VALIDADO |
| 7. Envelope inválido em DLQ com razão rastreável               | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO E VALIDADO |
| 8. Falha transitória sem ACK prematuro                         | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO E VALIDADO |
| 9. Crash pós-commit/pré-ACK e restart                          | `process-crash-recovery.test.ts`                                                                                                                       | IMPLEMENTADO E VALIDADO |
| 10. Pending reference, backoff e resolução                     | `distributed-delivery.test.ts`                                                                                                                         | IMPLEMENTADO E VALIDADO |
| 11. Restart retoma pending e outbox                            | `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`                                                                                       | IMPLEMENTADO E VALIDADO |
| 12. Reconciliação ao fim de cada cenário financeiro            | `financial-core.test.ts`, `concurrency.test.ts`, `distributed-delivery.test.ts`, `process-crash-recovery.test.ts`, `three-process-wallet-race.test.ts` | IMPLEMENTADO E VALIDADO |

## Resultado

**PRONTO PARA SUBMISSÃO.** Todas as evidências obrigatórias, inclusive
PostgreSQL, LocalStack/SQS, Compose, múltiplos processos, crash/restart e
reconciliação, foram executadas com sucesso. Um PostgreSQL externo na porta
`5432` não afeta a stack: Compose, runtime e testes usam `55432` por padrão.
